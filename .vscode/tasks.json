{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Blender",
            "type": "shell",
            "command": "blender --background --python scripts/genTrainScene.py",
            "options": {
                "cwd": "${workspaceFolder}/blender"
            },
            "problemMatcher": []
        }
    ]
}